services:
  dporter:
    image: ghcr.io/linuxsuren/dporter:latest
    container_name: dporter
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

  # Enable Linux user authentication:
  #   command: ["-auth"]
  #   volumes:
  #     - /etc/shadow:/etc/shadow:ro
  #     - /etc/passwd:/etc/passwd:ro
